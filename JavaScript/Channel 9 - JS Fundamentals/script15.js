


$(function() {

  /*Adding a CSS in a HTML using JQuery example bellow
  .highlight {
  background-color: yellow;
  }
  */

  // By ID addint to #first id
  // $("#first").addClass("highlight");

  // Seleciona todos os items que são pares. zero base
  // $("li:even").addClass("highlight");

  // Seleciona todos os items que são impares. zero base
  // $("li:odd").addClass("highlight");

  // By element tag adding to all paragraphs (p)
  // $("p").addClass("highlight");

  // By class addint to chosen
  // $(".chosen").addClass("highlight");

  // Combining two elements
  // $("#first, .chosen").addClass("highlight");

  // Contains a palavra 3 dentro de um elemento LI
  // $('li:contains("Three")').addClass('highlight');

  // next, previous items
  // $('li:contains("Three")').next().addClass('highlight');
  // $('li:contains("Three")').prev().addClass('highlight');

  // siblings todos menos ele, todos LI´s menos ele
  // $('li:contains("Three")').siblings().addClass('highlight');

  // parent seleciona toda a lista que estamos trabalhando
  // $('li:contains("Three")').parent().addClass('highlight');

  // Estamos procurando em qualquer lista o primeiro filho desta lista
  // $("li:nth-child(1)").addClass("highlight");

  // Select by Attribute, neste caso procuramos um paragrafo com nome MySecondPara
  // $('p[name="mySecondPara"]').addClass("highlight");

  // Agora o que não tem .
  // $('p[name!="mySecondPara"]').addClass("highlight");

  // Me de todos os paragrafos que NAO tenha uma id name atribuido
  // $("p").not('[name]').addClass("highlight");

  // Seleciona qualquer coisa em termos de H1.H2.H3.H4.H5.H6
  // $(":header").addClass("highlight");

  // Seleciona todos os paragrafos que estiverem vazios e adiciona um texto
  // $("p:empty").text("You seemed lonely");




});






  // By id
  //$('#first').addClass('highlight');

  // By element/ tag
  //$('p').addClass('highlight');

  // By class
  //$('.chosen').addClass('highlight');

  // Combination
  //$('#first, .chosen').addClass('highlight');  

  // Contains
  //$('li:contains("Three")').addClass('highlight');

  // $('li:odd').addClass('highlight');

  // next, previous
  //$('li:contains("Three")').prev().addClass('highlight');  

  // siblings, parent
  //$('li:contains("Three")').parent().addClass('highlight');

  //$('li:nth-child(1)').addClass('highlight');

  // attribute
  //$('p[name="mySecondPara"]').addClass('highlight');

  //$('p[name!="mySecondPara"]').addClass('highlight');

  //$('p').not('[name]').addClass('highlight');

  //$(':header').addClass('highlight');

  //$('p:empty').text('You seemed lonely so I gave you some text');








